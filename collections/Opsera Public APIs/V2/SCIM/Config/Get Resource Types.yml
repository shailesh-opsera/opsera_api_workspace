info:
  name: Get Resource Types
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/scim/v2/ResourceTypes"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
