info:
  name: Get Parameter Value
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/parameters/<parameterId>/value"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
