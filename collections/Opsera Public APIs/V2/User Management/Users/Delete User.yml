info:
  name: Delete User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/user/<userId>"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
