info:
  name: Get Project By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/data/project/<projectId>"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
