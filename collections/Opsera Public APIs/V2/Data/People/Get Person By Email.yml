info:
  name: Get Person By Email
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/data/people/<email>"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
